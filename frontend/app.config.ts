
export default defineAppConfig({
  nuxtIcon: {
    size: '1.5em', // default <Icon> size applied
    class: '', // default <Icon> class applied
    aliases: {
      'nuxt': 'logos:nuxt-icon',
    }
  }
})